{ // inspiration from: https://github.com/topics/waybar
  "layer": "top",
  "position": "top",

  "modules-left": ["hyprland/workspaces"],
  "modules-center": ["clock"],
  "modules-right": ["privacy", "tray", "custom/systemd-failed", "temperature", "cpu", "memory", "disk", "pulseaudio", "custom/mic", "network", "battery"],

  "clock": {
    "format": "{:%a %d %b  %H:%M}",
    "tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>"
  },

  "custom/systemd-failed": {
    "exec": "~/.config/waybar/scripts/failed-units.sh",
    "interval": 60,
    "format": "Sysd fails: {}",
    "on-click": "$TERMINAL -e sh -c 'echo SYSTEM; systemctl --failed; echo; echo USER; systemctl --user --failed 2>/dev/null; echo; read'"
  },

  "pulseaudio": {
    "format": "Vol {volume}%",
    "format-muted": "Mute",
    "on-click": "pavucontrol",
    "scroll-step": 5
  },

  "custom/mic": {
    "exec": "pactl get-source-mute @DEFAULT_SOURCE@ | grep -q yes && echo 'Mic: muted' || echo 'Mic: active'",
    "on-click": "pavucontrol",
    "interval": 1
  },

  "network": {
    "interval": 5,
    "format-ethernet": "󰈀  LAN",
    "format-wifi": "  {essid}",
    "format-disconnected": "󰤮",
    "tooltip-format-ethernet": "Ethernet: {ifname}\nIP: {ipaddr}\nDown: {bandwidthDownBits} Up: {bandwidthUpBits}",
    "on-click": "$TERMINAL -e sh -c '~/.config/waybar/scripts/net_info.sh; read'"
  },

  "battery": {
    "states": {
      "warning": 30,
      "critical": 15
    },
    "format": "{icon} {capacity}%",
    "format-charging": "󰂄 {capacity}%",
    "format-plugged": "󰂄 {capacity}%",
    "format-alt": "{icon} {time}",
    "format-full": "󱈑 {capacity}%",
    "format-icons": ["󱊡", "󱊢", "󱊣"]
  },

  "temperature": {
    //"thermal-zone": 1, // less accurate
    "hwmon-path": "/sys/devices/platform/coretemp.0/hwmon/hwmon4/temp1_input", // most acc key but value might be diff for you.
    "critical-threshold": 80,
    "format-critical": "{temperatureC}°C ⚠️",
    "format": "{temperatureC}°C"
  },

  "cpu": {
    "format": "CPU {usage}% ({load})",
    "interval": 1,
    "on-click": "$TERMINAL -e top"
  },

  "memory": {
    "format": "{used:0.1f}G/{total:0.1f}G  [{swapUsed}/{swapTotal}]",
    "on-click": "$TERMINAL -e bash -c 'free -h; read'"
  },

  "disk": {
    "interval": 60,
    "format": "Disk {percentage_used}%",
    "path": "/",
    "on-click": "$TERMINAL -e sh -c 'df -h; echo; read'"
  },

  "privacy": {
    "icon-spacing": 4,
    "icon-size": 18,
    "transition-duration": 250,
    "modules": [
      {
        "type": "screenshare",
        "tooltip": true,
        "tooltip-icon-size": 20
      },
      {
        "type": "audio-out",
        "tooltip": true,
        "tooltip-icon-size": 20
      },
      {
        "type": "audio-in",
        "tooltip": true,
        "tooltip-icon-size": 20
      }
    ]
  }
}
